const artigos = require('./data/artigos');
const comentarios = require('./data/comentarios');
const favoritados = require('./data/favoritados');
const jogos = require('./data/jogos');
const usuarios = require('./data/usuarios');

module.exports = {
  artigos,
  comentarios,
  favoritados,
  jogos,
  usuarios
}